name: Publish UiPath Solution

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  # ============================================
  # STAGE 1: Deploy to Staging
  # ============================================
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    outputs:
      package-version: ${{ steps.set-version.outputs.version }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set version
        id: set-version
        run: echo "version=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install UiPath CLI (Latest - Linux)
        run: |
          dotnet tool install --global UiPath.CLI.Linux
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
          uipcli --version

      - name: Pack Solution
        run: |
          uipcli solution pack . \
            --output ./packages \
            --version "${{ steps.set-version.outputs.version }}"
          echo "Package contents:"
          ls -la ./packages/

      - name: Upload Solution Package to Staging
        run: |
          PACKAGE_FILE=$(find ./packages -name "*.zip" | head -1)
          echo "Uploading package: $PACKAGE_FILE"
          uipcli solution upload-package "$PACKAGE_FILE" \
            -U "${{ vars.ORCHESTRATOR_URL }}" \
            -T "${{ vars.ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.ORG_NAME }}" \
            -I "${{ secrets.EXTERNAL_APP_ID }}" \
            -S "${{ secrets.EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Packages Solutions.Packages.Write"

      - name: Deploy Solution to Staging
        run: |
          uipcli solution deploy "CityBreak Process" \
            -v "${{ steps.set-version.outputs.version }}" \
            -d "CityBreak-Process-v${{ steps.set-version.outputs.version }}" \
            -f "${{ vars.ORCHESTRATOR_FOLDER }}" \
            --deploymentParentFolder "${{ vars.DEPLOYMENT_PARENT_FOLDER }}" \
            -U "${{ vars.ORCHESTRATOR_URL }}" \
            -T "${{ vars.ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.ORG_NAME }}" \
            -I "${{ secrets.EXTERNAL_APP_ID }}" \
            -S "${{ secrets.EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Deployments Solutions.Deployments.Read Solutions.Deployments.Write Solutions.Packages Solutions.Packages.Read Solutions.Packages.Write"

      - name: Activate Staging Deployment
        run: |
          uipcli solution deploy-activate "CityBreak-Process-v${{ steps.set-version.outputs.version }}" \
            -U "${{ vars.ORCHESTRATOR_URL }}" \
            -T "${{ vars.ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.ORG_NAME }}" \
            -I "${{ secrets.EXTERNAL_APP_ID }}" \
            -S "${{ secrets.EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Deployments Solutions.Deployments.Read Solutions.Deployments.Write Solutions.Packages Solutions.Packages.Read Solutions.Packages.Write"

      - name: Staging Summary
        if: always()
        run: |
          echo "## Staging Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Solution: CityBreak Process" >> $GITHUB_STEP_SUMMARY
          echo "- Version: ${{ steps.set-version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "- Tenant: ${{ vars.ORCHESTRATOR_TENANT }}" >> $GITHUB_STEP_SUMMARY
          echo "- Parent Folder: ${{ vars.DEPLOYMENT_PARENT_FOLDER }}" >> $GITHUB_STEP_SUMMARY
          echo "- Deployment Folder: ${{ vars.ORCHESTRATOR_FOLDER }}" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # STAGE 2: Deploy to Production (with approval)
  # ============================================
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: deploy-staging
    environment: Production  # Requires approval configured in GitHub
    
    steps:
      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install UiPath CLI (Latest - Linux)
        run: |
          dotnet tool install --global UiPath.CLI.Linux
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH
          uipcli --version

      - name: Download Package from Staging
        run: |
          mkdir -p ./packages
          uipcli solution download-package "CityBreak Process" \
            -v "${{ needs.deploy-staging.outputs.package-version }}" \
            --output ./packages \
            -U "${{ vars.ORCHESTRATOR_URL }}" \
            -T "${{ vars.ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.ORG_NAME }}" \
            -I "${{ secrets.EXTERNAL_APP_ID }}" \
            -S "${{ secrets.EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Packages Solutions.Packages.Read"
          echo "Downloaded package:"
          ls -la ./packages/

      - name: Upload Package to Production
        run: |
          PACKAGE_FILE=$(find ./packages -name "*.zip" | head -1)
          echo "Uploading package to Production: $PACKAGE_FILE"
          uipcli solution upload-package "$PACKAGE_FILE" \
            -U "${{ vars.PROD_ORCHESTRATOR_URL }}" \
            -T "${{ vars.PROD_ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.PROD_ORG_NAME }}" \
            -I "${{ secrets.PROD_EXTERNAL_APP_ID }}" \
            -S "${{ secrets.PROD_EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Packages Solutions.Packages.Write"

      - name: Deploy Solution to Production
        run: |
          uipcli solution deploy "CityBreak Process" \
            -v "${{ needs.deploy-staging.outputs.package-version }}" \
            -d "CityBreak-Process-v${{ needs.deploy-staging.outputs.package-version }}" \
            -f "${{ vars.PROD_ORCHESTRATOR_FOLDER }}" \
            --deploymentParentFolder "${{ vars.PROD_DEPLOYMENT_PARENT_FOLDER }}" \
            -U "${{ vars.PROD_ORCHESTRATOR_URL }}" \
            -T "${{ vars.PROD_ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.PROD_ORG_NAME }}" \
            -I "${{ secrets.PROD_EXTERNAL_APP_ID }}" \
            -S "${{ secrets.PROD_EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Deployments Solutions.Deployments.Read Solutions.Deployments.Write Solutions.Packages Solutions.Packages.Read Solutions.Packages.Write"

      - name: Activate Production Deployment
        run: |
          uipcli solution deploy-activate "CityBreak-Process-v${{ needs.deploy-staging.outputs.package-version }}" \
            -U "${{ vars.PROD_ORCHESTRATOR_URL }}" \
            -T "${{ vars.PROD_ORCHESTRATOR_TENANT }}" \
            -A "${{ vars.PROD_ORG_NAME }}" \
            -I "${{ secrets.PROD_EXTERNAL_APP_ID }}" \
            -S "${{ secrets.PROD_EXTERNAL_APP_SECRET }}" \
            --applicationScope "AutomationSolutions Solutions.Deployments Solutions.Deployments.Read Solutions.Deployments.Write Solutions.Packages Solutions.Packages.Read Solutions.Packages.Write"

      - name: Production Summary
        if: always()
        run: |
          echo "## Production Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Solution: CityBreak Process" >> $GITHUB_STEP_SUMMARY
          echo "- Version: ${{ needs.deploy-staging.outputs.package-version }}" >> $GITHUB_STEP_SUMMARY
          echo "- Tenant: ${{ vars.PROD_ORCHESTRATOR_TENANT }}" >> $GITHUB_STEP_SUMMARY
          echo "- Parent Folder: ${{ vars.PROD_DEPLOYMENT_PARENT_FOLDER }}" >> $GITHUB_STEP_SUMMARY
          echo "- Deployment Folder: ${{ vars.PROD_ORCHESTRATOR_FOLDER }}" >> $GITHUB_STEP_SUMMARY
